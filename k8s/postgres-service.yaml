apiVersion: v1
kind: Service
metadata:
  name: conevent-postgres
  labels:
    app: conevent-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: conevent-postgres